# Optional. The localhost URL is used automatically during local development.
VITE_RPC_URL=http://127.0.0.1:8545

# Optional overrides. The deployment script writes the current addresses to
# src/contracts/deployment.json automatically.
VITE_REGISTRY_ADDRESS=
VITE_TOKEN_ADDRESS=
